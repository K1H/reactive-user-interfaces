import React, { Component } from 'react';
import './App.css';
import Button from './Button'

class App extends Component {
  render() {
    return (
    const before = ['thing1', 'thing2', 'thing3'];

    const after = before.map((label, i) => {return <p key = {i}>{label}</p>;})
    );
return <div className= "App">{after</div>}

  }
}

export default App;
